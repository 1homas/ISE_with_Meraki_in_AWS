---
repositories:
  - name: FTP
    serverName: "{{ lookup('env', 'ISE_REPOSITORY') }}"
    protocol: FTP
    path: /
    userName: "{{ lookup('env', 'ISE_REPOSITORY_USERNAME') }}"
    password: "{{ lookup('env', 'ISE_REPOSITORY_PASSWORD') }}"
